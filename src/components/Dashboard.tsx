import { Card, CardContent, CardHeader } from '@mui/material';

export const Dashboard = () => (
    <Card>
        <CardHeader title="Processed notifications" />
        <CardContent>Put some charts here...</CardContent>
    </Card>
);
